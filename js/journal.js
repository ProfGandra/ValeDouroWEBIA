// ValeDouro WEBIA — Diário diegético do personagem
(function(){
'use strict';
const KEY='valedouro.journal.v1';
const BASE_CATALOG='data/journal/base-entries.json';
const NOTEBOOK_RE=/caderno|di[aá]rio|notebook|livro\s*de\s*notas/i;
let catalog={},pageCursor=0;
const esc=s=>window.esc?window.esc(String(s??'')):String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const charKey=c=>`${c?.name||'sem-nome'}|${c?.cls||''}`;
function readAll(){try{const x=JSON.parse(localStorage.getItem(KEY)||'{}');return x&&typeof x==='object'?x:{}}catch{return {}}}
function writeAll(x){localStorage.setItem(KEY,JSON.stringify(x));window.dispatchEvent(new CustomEvent('valedouro:journal-change'))}
function stateFor(c){const all=readAll(),k=charKey(c);if(!all[k])all[k]={version:1,active:false,boundItemId:null,boundItemName:null,startedAt:null,entries:[],seenEntryIds:[],updatedAt:null};return {...all[k],entries:Array.isArray(all[k].entries)?all[k].entries:[],seenEntryIds:Array.isArray(all[k].seenEntryIds)?all[k].seenEntryIds:[]}}
function saveFor(c,j){const all=readAll(),k=charKey(c);j.updatedAt=new Date().toISOString();all[k]=j;writeAll(all);return j}
function inventoryFor(c){try{if(window.ValeInventory?.ensure)return window.ValeInventory.ensure(c)}catch{}try{const db=JSON.parse(localStorage.getItem('valedouro.inventory.v2')||'{}');return db[charKey(c)]||null}catch{return null}}
function allItems(c){const inv=inventoryFor(c);return [...(inv?.items||[]),...(inv?.resources||[])]}
function isNotebook(it){return !!it&&Number(it.qty||0)>0&&!['lost','abandoned'].includes(it.state)&&((it.journalCapable===true)||NOTEBOOK_RE.test(String(it.name||''))||NOTEBOOK_RE.test(String(it.id||'')))}
function compatibleItems(c){return allItems(c).filter(isNotebook)}
function boundAvailable(c,j){if(!j.active||!j.boundItemId)return false;return isNotebook(allItems(c).find(x=>x.id===j.boundItemId))}
function activeCharacter(){return window.state?.characters?.[window.state?.active||0]||null}
function bind(c,itemId){const item=compatibleItems(c).find(x=>x.id===itemId);if(!item)return {ok:false,reason:'item-unavailable'};const j=stateFor(c);j.active=true;j.boundItemId=item.id;j.boundItemName=item.name||'Caderno';j.startedAt=j.startedAt||new Date().toISOString();saveFor(c,j);pageCursor=0;syncButton();open();return {ok:true}}
function unbind(c){const j=stateFor(c);j.active=false;j.boundItemId=null;j.boundItemName=null;saveFor(c,j);syncButton()}
function addEntry(c,e){if(!c||!e)return false;const j=stateFor(c);if(!j.active||!boundAvailable(c,j))return false;const id=String(e.id||`entry-${Date.now()}`);if(j.seenEntryIds.includes(id))return false;j.entries.push({id,at:e.at||new Date().toISOString(),title:e.title||'Registro',body:e.body||'',learnings:Array.isArray(e.learnings)?e.learnings:[],people:Array.isArray(e.people)?e.people:[],places:Array.isArray(e.places)?e.places:[],illustration:e.illustration||null,source:e.source||null,questId:e.questId||null});j.seenEntryIds.push(id);j.seenEntryIds=j.seenEntryIds.slice(-1000);saveFor(c,j);return true}
function registerCatalog(entries){if(Array.isArray(entries))entries.forEach(e=>{if(e?.questId)catalog[e.questId]=e});else if(entries&&typeof entries==='object')Object.assign(catalog,entries)}
async function loadBaseCatalog(){try{const r=await fetch(BASE_CATALOG,{cache:'no-store'});if(r.ok)registerCatalog(await r.json())}catch(e){console.warn('Diário: catálogo-base indisponível',e)}}
async function recordQuest(id){const tpl=catalog[id];if(!tpl)return;for(const c of (window.state?.characters||[])){const j=stateFor(c);if(j.active&&boundAvailable(c,j))addEntry(c,{...tpl,id:`quest-${id}`,questId:id,source:'quest'})}}
function publicState(){return (window.state?.characters||[]).map(c=>{const j=stateFor(c);return {character:c.name,enabled:!!j.active,available:boundAvailable(c,j),boundItemName:j.boundItemName,startedAt:j.startedAt,entryCount:j.entries.length,lastEntry:j.entries.at(-1)?.title||null}})}
function formatDate(iso){try{return new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'short',year:'numeric'}).format(new Date(iso))}catch{return ''}}
function entryHtml(e,compact=false){const learns=e.learnings?.length?`<div class="journal-learnings"><strong>Aprendizados</strong><ul>${e.learnings.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`:'';return `<article class="journal-note ${compact?'compact':''}"><div class="journal-date">${esc(formatDate(e.at))}</div><h3>${esc(e.title)}</h3><p>${esc(e.body).replace(/\n/g,'<br>')}</p>${learns}</article>`}
function sketchHtml(e){return `<div class="journal-sketch-page"><img src="${esc(e.illustration)}" alt="Esboço a grafite: ${esc(e.title)}" onerror="this.parentElement.style.display='none'"><div class="journal-sketch-caption">${esc(e.title)}</div></div>`}
function weight(e){return String(e.body||'').length+(e.learnings?.length||0)*85+(e.title?.length||0)*2}
function pagesFor(c,j){const pages=[{kind:'cover',html:`<div class="journal-cover-page"><div class="quill">✒</div><h2>${esc(j.boundItemName||'Diário')}</h2><div class="owner">${esc(c.name)}</div><p>Registros de viagem, descobertas e aprendizados.</p><small>Iniciado em ${esc(formatDate(j.startedAt))}</small></div>`}];if(!j.entries.length){pages.push({kind:'blank',html:'<div class="journal-blank">As páginas ainda estão em branco.</div>'});return pages}
const indexPage={kind:'index',html:''};pages.push(indexPage);const entryPageMap=[];for(let i=0;i<j.entries.length;i++){const e=j.entries[i];entryPageMap[i]=pages.length+1;if(e.illustration){pages.push({kind:'entry',html:entryHtml(e)});pages.push({kind:'sketch',html:sketchHtml(e)});continue}const next=j.entries[i+1];if(next&&!next.illustration&&weight(e)<520&&weight(next)<520){pages.push({kind:'entry',html:`${entryHtml(e,true)}<div class="journal-note-divider"></div>${entryHtml(next,true)}`});entryPageMap[i+1]=pages.length;i++;}else pages.push({kind:'entry',html:entryHtml(e)})}
indexPage.html=`<h3>Índice</h3><ul class="journal-index">${j.entries.map((e,i)=>`<li><button onclick="ValeJournal.goToEntry(${i})">${esc(e.title)}</button><span>${entryPageMap[i]||''}</span></li>`).join('')}</ul>`;return pages}
function ensureUI(){if(document.getElementById('journalBtn'))return;const style=document.createElement('style');style.textContent=`#journalBtn[disabled]{opacity:.45;cursor:not-allowed}.journal-modal{background:rgba(5,4,3,.88)}.journal-modal .panel{width:min(1180px,97vw);max-height:94vh;overflow:hidden;background:transparent!important;box-shadow:none!important;border:0!important;padding:0!important}.journal-head{display:flex;justify-content:space-between;align-items:center;margin:0 0 10px;color:#eadfce}.journal-book{position:relative;margin:auto;width:min(1100px,95vw);height:min(720px,78vh);display:grid;grid-template-columns:1fr 1fr;background:#4a3420;border:16px solid #3a2515;border-radius:18px 22px 22px 18px;box-shadow:0 24px 70px rgba(0,0,0,.7),inset 0 0 0 2px #8c6840;perspective:1800px;overflow:hidden}.journal-book:after{content:'';position:absolute;left:50%;top:0;bottom:0;width:18px;transform:translateX(-50%);background:linear-gradient(90deg,rgba(0,0,0,.35),rgba(255,255,255,.08),rgba(0,0,0,.28));z-index:5;pointer-events:none}.journal-page{position:relative;overflow:auto;padding:42px 44px 54px;color:#382f24;background:radial-gradient(circle at 30% 20%,rgba(255,255,255,.35),transparent 28%),linear-gradient(90deg,#e9dfc5,#f4ecd9 10%,#efe5ce 88%,#d9caaa);font-family:Georgia,'Times New Roman',serif;box-shadow:inset 0 0 35px rgba(95,71,38,.12)}.journal-page h3{font-size:25px;font-weight:500;color:#44311e;margin-top:0}.journal-page p{font-size:17px;line-height:1.62}.journal-page ul{font-size:15px;line-height:1.5;padding-left:22px}.journal-date{font-size:12px;letter-spacing:.06em;text-transform:uppercase;opacity:.62;margin-bottom:12px}.journal-note.compact h3{font-size:21px}.journal-note.compact p{font-size:15px;line-height:1.48}.journal-note-divider{margin:22px 12%;border-top:1px solid rgba(65,47,29,.25)}.journal-sketch-page{height:100%;display:flex;flex-direction:column;align-items:center;justify-content:center}.journal-sketch-page img{width:96%;max-height:82%;object-fit:contain;mix-blend-mode:multiply;filter:grayscale(1) contrast(1.08);opacity:.92}.journal-sketch-caption{margin-top:10px;font-style:italic;opacity:.6}.journal-pageno{position:absolute;bottom:18px;left:0;right:0;text-align:center;font-size:12px;opacity:.55}.journal-blank{height:100%;display:flex;align-items:center;justify-content:center;text-align:center;font-style:italic;opacity:.48}.journal-cover-page{height:100%;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center}.journal-cover-page h2{font-size:34px;margin-bottom:8px}.journal-cover-page .owner{font-size:20px;margin-top:18px}.quill{font-size:48px}.journal-nav{display:flex;justify-content:center;align-items:center;gap:14px;margin-top:10px}.journal-counter{min-width:150px;text-align:center;color:#d8c8aa;font-size:13px}.journal-bind-list{display:grid;gap:10px;background:#19140f;padding:20px;border-radius:14px}.journal-bind-card{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:12px;border:1px solid rgba(212,184,130,.25);border-radius:10px}.journal-empty{padding:28px;text-align:center;color:#d8c8aa}.journal-index{list-style:none;padding:0!important}.journal-index li{display:flex;justify-content:space-between;border-bottom:1px dotted rgba(60,45,30,.35);padding:7px 0;gap:12px}.journal-index button{all:unset;cursor:pointer;flex:1}.journal-index button:hover{text-decoration:underline}@keyframes pageNext{0%{transform:rotateY(-7deg);opacity:.82}100%{transform:rotateY(0);opacity:1}}@keyframes pagePrev{0%{transform:rotateY(7deg);opacity:.82}100%{transform:rotateY(0);opacity:1}}.turn-next{animation:pageNext .28s ease}.turn-prev{animation:pagePrev .28s ease}@media(max-width:760px){.journal-book{grid-template-columns:1fr;height:min(760px,80vh);border-width:10px}.journal-book:after{display:none}.journal-page{padding:30px 26px 48px}.journal-page.mobile-hidden{display:none}.journal-head h2{font-size:18px}}`;document.head.appendChild(style);const gameButtons=document.querySelector('#game > .topbar > .row');if(gameButtons){const b=document.createElement('button');b.id='journalBtn';b.className='btn small';b.textContent='📖 Diário';b.onclick=()=>open();gameButtons.insertBefore(b,gameButtons.firstChild)}const modal=document.createElement('div');modal.id='journalModal';modal.className='modal journal-modal';modal.innerHTML='<div class="panel"><div class="journal-head"><h2>📖 Diário</h2><button class="btn" onclick="ValeJournal.close()">Fechar</button></div><div id="journalContent"></div></div>';document.body.appendChild(modal);syncButton()}
function syncButton(){const b=document.getElementById('journalBtn'),c=activeCharacter();if(!b||!c)return;const j=stateFor(c),has=compatibleItems(c).length>0,avail=boundAvailable(c,j);b.disabled=!avail&&!has;b.title=avail?`Diário: ${j.boundItemName||'caderno'}`:has?'Você possui um caderno que pode ser usado como Diário.':'Você ainda não possui um caderno para usar como Diário.';b.textContent=avail?'📖 Diário':has?'📖 Diário':'📖 Diário 🔒'}
function isMobile(){return window.matchMedia('(max-width:760px)').matches}
function renderBook(direction=''){const c=activeCharacter();if(!c)return;const j=stateFor(c),host=document.getElementById('journalContent'),pages=pagesFor(c,j),step=isMobile()?1:2;pageCursor=Math.max(0,Math.min(pageCursor,Math.max(0,pages.length-step)));if(!isMobile()&&pageCursor%2)pageCursor--;const empty={html:''},left=pages[pageCursor]||empty,right=pages[pageCursor+1]||empty,anim=direction==='next'?'turn-next':direction==='prev'?'turn-prev':'';const page=(p,n,side)=>`<section class="journal-page ${side} ${anim} ${side==='right'&&isMobile()?'mobile-hidden':''}">${p.html}${p.html?`<div class="journal-pageno">${n}</div>`:''}</section>`;host.innerHTML=`<div class="journal-book">${page(left,pageCursor+1,'left')}${page(right,pageCursor+2,'right')}</div><div class="journal-nav"><button class="btn" ${pageCursor<=0?'disabled':''} onclick="ValeJournal.prevPage()">← Anterior</button><div class="journal-counter">${pageCursor+1}${isMobile()?'':`–${Math.min(pageCursor+2,pages.length)}`} de ${pages.length}</div><button class="btn" ${pageCursor+step>=pages.length?'disabled':''} onclick="ValeJournal.nextPage()">Próxima →</button></div>`}
function nextPage(){const c=activeCharacter(),step=isMobile()?1:2,pages=c?pagesFor(c,stateFor(c)):[];if(pageCursor+step<pages.length){pageCursor+=step;renderBook('next')}}function prevPage(){const step=isMobile()?1:2;if(pageCursor>0){pageCursor=Math.max(0,pageCursor-step);renderBook('prev')}}function goToEntry(index){const c=activeCharacter();if(!c)return;const j=stateFor(c),pages=pagesFor(c,j);let target=pages.findIndex(p=>p.kind==='entry'&&p.html.includes(esc(j.entries[index]?.title||'')));if(target<0)target=2;if(!isMobile())target-=target%2;pageCursor=target;renderBook('next')}
function open(){ensureUI();const c=activeCharacter();if(!c)return;const host=document.getElementById('journalContent'),j=stateFor(c),items=compatibleItems(c);if(!j.active||!boundAvailable(c,j)){if(!items.length){host.innerHTML='<div class="journal-empty"><h3>Diário indisponível</h3><p>Você ainda não possui um caderno que possa ser usado como Diário.</p></div>';document.getElementById('journalModal').classList.add('active');return}host.innerHTML=`<div class="journal-bind-list"><h3>Escolha um caderno</h3><p>Os registros começam somente depois que você decide usar um caderno como Diário.</p>${items.map(it=>`<div class="journal-bind-card"><div><strong>${esc(it.name)}</strong></div><button class="btn primary" onclick="ValeJournal.bindActive(${JSON.stringify(it.id)})">Usar como Diário</button></div>`).join('')}</div>`;document.getElementById('journalModal').classList.add('active');return}pageCursor=0;renderBook();document.getElementById('journalModal').classList.add('active')}
function close(){document.getElementById('journalModal')?.classList.remove('active')}function bindActive(id){const c=activeCharacter();if(c)bind(c,id)}
const previousFetch=window.fetch.bind(window);window.fetch=async function(input,init){const url=typeof input==='string'?input:(input?.url||'');if((typeof AI_ENDPOINT!=='undefined'&&url===AI_ENDPOINT)&&init?.method==='POST'&&init.body){try{const b=JSON.parse(init.body);b.state={...(b.state||{}),journal:publicState()};init={...init,body:JSON.stringify(b)}}catch{}}const res=await previousFetch(input,init);try{const data=await res.clone().json();if(typeof data?.text==='string'&&data.text.includes('[[JOURNAL:')){let changed=false;data.text=data.text.replace(/\s*\[\[JOURNAL:([^\]]+)\]\]\s*/gi,(_,payload)=>{try{const e=JSON.parse(decodeURIComponent(payload)),c=window.state?.characters?.[Number.isInteger(e.characterIndex)?e.characterIndex:(window.state?.active||0)];if(c&&addEntry(c,e))changed=true}catch{}return ' '});if(changed){data.text=data.text.trim();const h=new Headers(res.headers);h.set('Content-Type','application/json; charset=utf-8');return new Response(JSON.stringify(data),{status:res.status,statusText:res.statusText,headers:h})}}}catch{}return res};
window.ValeJournal={stateFor,bind,bindActive,unbind,addEntry,recordQuest,registerCatalog,compatibleItems,publicState,open,close,syncButton,nextPage,prevPage,goToEntry};window.openJournal=open;window.addEventListener('valedouro:quest-complete',e=>recordQuest(e.detail?.completed));window.addEventListener('valedouro:inventory-change',syncButton);window.addEventListener('valedouro:journal-change',syncButton);window.addEventListener('DOMContentLoaded',()=>{ensureUI();loadBaseCatalog();setInterval(syncButton,1800)});if(document.readyState!=='loading'){ensureUI();loadBaseCatalog();setInterval(syncButton,1800)}
})();
